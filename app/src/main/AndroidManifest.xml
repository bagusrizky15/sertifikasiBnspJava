<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.ariflaksito.mystudents">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyStudents">
        <activity
            android:name=".UpdateKeluarActivity"
            android:exported="false" />
        <activity
            android:name=".KeluarActivity"
            android:exported="false" />
        <activity android:name=".user.db.LoginActivity" />
        <activity android:name=".BerandaActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".user.db.RegisterActivity" />
        <activity
            android:name=".SettingActivity"
            android:exported="false" />
        <activity android:name=".UpdateActivity" />
        <activity android:name=".ListStudentsActivity" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>